[dataset]
name = "bigcode"
metric = "l2"
degree = 64
main_dimension = 256

[paths]
base = "/data/raid0/bigcode/bigcode_base.fbin"
query = "/data/raid0/bigcode/bigcode_query.fbin"
gt = "/data/raid0/bigcode/bigcode_gt"
vamana = "/data/raid0/bigcode/index_bigcode_base_R64_L200_A1.2"
output = "/md1/huangliang/alaya-dev/build_graph/laser_port/bigcode"

# All fields below are optional (defaults shown)
# [build]
# build_threads = 48   # threads for index construction
# ef_indexing = 200    # ef parameter for index construction

# [search]
# topk = 10
# threads = 1
# beam_width = 16
# dram_budget = 1.0
# ep_num = 300
# efs = [80, 90, 100, 110, 130, 150, 200, 250, 300, 400, 500]
